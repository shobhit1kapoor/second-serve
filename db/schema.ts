import { index, integer, sqliteTable, text } from 'drizzle-orm/sqlite-core';
export const runs = sqliteTable('runs', { id: text('id').primaryKey(), owner: text('owner').notNull(), startedAt: integer('started_at').notNull(), updatedAt: integer('updated_at').notNull(), status: text('status').notNull(), mode: text('mode').notNull(), payload: text('payload').notNull() }, table => [index('runs_owner_started').on(table.owner, table.startedAt), index('runs_started').on(table.startedAt)]);
export const commands = sqliteTable('commands', { id: text('id').primaryKey(), runId: text('run_id').notNull().references(() => runs.id), createdAt: integer('created_at').notNull(), handled: integer('handled').notNull().default(0), payload: text('payload').notNull() }, table => [index('commands_run_pending').on(table.runId, table.handled)]);
